import express from 'express';
//import {getPortefeuille} from '../controllers/user.controller.js';
//import { authenticateToken } from '../middlewares/authenticateToken.js';

const router = express.Router();


export default router;